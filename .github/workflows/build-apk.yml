name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Build Vite app
        run: npm run build
        working-directory: frontend

      - name: Sync Capacitor
        run: |
          npx cap sync android --verbose > sync.log 2>&1 || (cat sync.log && exit 1)
        working-directory: frontend

      - name: Upload Sync Log
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: capacitor-sync-log
          path: frontend/sync.log

      - name: Push debug logs to branch
        if: failure()
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git checkout -b debug-logs || git checkout debug-logs
          cp frontend/sync.log sync.log || true
          git add sync.log || true
          git commit -m "debug: sync failure logs [skip ci]" || true
          git push origin debug-logs --force

      - name: Make Gradle executable
        run: chmod +x frontend/android/gradlew

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon
        working-directory: frontend/android
        env:
          ANDROID_HOME: ${{ env.ANDROID_SDK_ROOT }}

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: AeroFarm-AI-debug
          path: frontend/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

      - name: Get APK size
        run: |
          APK_PATH="frontend/android/app/build/outputs/apk/debug/app-debug.apk"
          if [ -f "$APK_PATH" ]; then
            SIZE=$(du -h "$APK_PATH" | cut -f1)
            echo "✅ APK built successfully! Size: $SIZE"
          fi
